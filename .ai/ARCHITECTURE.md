# Architecture & repository map

> Maintained by coding agents. Update whenever structure or content changes. Protocol: see `CLAUDE.md`.

**Last updated:** 2026-07-03

## Overview

`mesoLTM` — purpose not yet documented (fill in once defined). Python 3.13 project, currently a skeleton with no application code.

## Top-level layout

- `CLAUDE.md` — agent operating manual and working protocol.
- `.ai/` — this AI tracking system (agent context; not user docs).
- `docs/` — reserved for user-facing documentation (not present yet).
- `README.md` — project readme / usage.
- `LICENSE` — MIT.
- `.gitignore` — Python template + `.DS_Store`.
- `venv/` — local virtualenv (git-ignored, Python 3.13.9).

## Components / modules

_None yet._ As code is introduced, list each module/package here with a one-line responsibility and its key entry points.

## Data flow / how it fits together

_N/A yet._ Describe the runtime flow (entry point → components → outputs) once there is code.
