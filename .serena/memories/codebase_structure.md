# Codebase Structure

`mesoltm` v0.1 is implemented: a mesoscopic (individual-vehicle) Link Transmission
Model ported faithfully from `abmmeso` with added network/routing/control layers.
See the **Deviations from the paper** docs page (`docs/model/deviations-from-the-paper.md`) for the (small) deviations from the reference model.

## Top-level
- `pyproject.toml` — hatchling, PyPI-ready, `requires-python>=3.11`, deps `numpy`+`networkx`+`tqdm` (tqdm powers the optional simulation progress bar); extras `plot`/`ui`/`calib`/`dev`/**`docs`** (`mkdocs-material`, `mkdocstrings[python]`, `griffe`); `mesoltm` console script.
- `LICENSE` — **AGPL-3.0** (was MIT); `NOTICE` — attribution to de Souza et al. + paper DOI.
- `src/mesoltm/` — the package; `examples/` — runnable scripts (outside src); `docs/` — the MkDocs user-doc sources (see **Documentation** below; the deviations record now lives in `docs/model/deviations-from-the-paper.md`, not a separate `MODEL_CHANGES.md`). `examples/paper_validation.py` is the canonical **paper-validation** script: one file reproducing the paper's Section 4 figures (Fig 5 lane-drop, 6 W=7 proposed-only, 7 diverge, 8 diverge-random, 9 merge) through the shipped discrete pipeline, with the deliberate divergences (discrete-only/no continuous ref, CFL rejection of Fig 5 dt=6 s, omitted `floor(C·dt)` ablation, out-of-scope signal/real-world Figs 11/12) surfaced on the figures — use it to sanity-check pipeline fidelity after core changes.
- **Documentation (two sets, added 2026-07-05):**
  - **User site** — MkDocs Material at `mkdocs.yml` (repo root) with sources under `docs/` (`index.md`, `getting-started/`, `model/` incl. `deviations-from-the-paper.md` — a **hand-authored** documentation page (prose A1–A8 / B1–B5 sections; rewritten 2026-07-06, no longer a snippet-include of `MODEL_CHANGES.md`), `guide/`, `reference/` = 10 `mkdocstrings` auto-pages, `about/` incl. `changelog.md` which snippet-includes the repo-root `CHANGELOG.md`). API reference auto-rendered from Google-style docstrings (`mkdocstrings[python]`, `merge_init_into_class`). `mkdocs.yml` enables `pymdownx.emoji` (Material `twemoji`/`to_svg`) so `:material-*:` icon shortcodes (home-page grid cards) render as SVG. Deployed to **GitHub Pages via the `gh-pages` branch** (`mkdocs gh-deploy --force`) by two workflows mirroring `sjschlapbach/traffic_flow_models`: `docs_build.yml` (build `--strict` on push to `master`/PRs) and `docs_release.yml` (deploy; trigger being tuned — tag `v*.*.*` vs push). `mkdocs_hooks.py` (repo root) raises griffe's loggers to ERROR so `--strict` ignores benign untyped-`ax`/`**kwargs` docstring warnings but still fails on broken links/anchors. Build output goes to git-ignored `site/`. (The old developer-facing `docs/MODEL_CHANGES.md` was **deleted 2026-07-06** — its content now lives solely in the hand-authored deviations page, no duplication.)
  - **AI-agent docs** — dense, Context7-optimised markdown under `ai-docs/` (index, installation, quickstart, model, networks, vehicles-and-routing, plugins, simulation, metrics, visualization, scenarios-cli, condensed `api-reference`, gotchas). Repo-root **`context7.json`** (`folders: ["ai-docs"]`, title/description/`rules`) tells Context7 to index that set. TODO: submit the repo at context7.com/add-library.
  - `CITATION.cff` (repo root) — CFF 1.2.0 with a **DOI placeholder + TODO** (referenced by `README`); paper as secondary reference. TODO: register a Zenodo DOI and fill it in.
- `.github/workflows/` — one workflow per gate: `lint.yml` (pylint), `format.yml` (black --check), `typecheck.yml` (mypy), `test.yml` (pytest, 3.11), `examples.yml` (runs every `examples/*.py`), `build.yml` (build sdist+wheel on push to master / PR), `test.yml` (pytest + a CLI smoke step), `docs_build.yml` (MkDocs build `--strict` on push/PR) + `docs_release.yml` (`gh-deploy`), and `release.yml` — an **automated release** triggered by a `v*.*.*` tag: validates the tag == `pyproject.toml` version, generates/updates `CHANGELOG.md` with **git-cliff** (config in `cliff.toml`), builds, verifies the built wheel's version, publishes to **PyPI** (`PYPI_TOKEN` secret), pushes the changelog commit to `master`, and creates a GitHub Release. Structure mirrors `sjschlapbach/traffic_flow_models`.
- `cliff.toml` — git-cliff changelog config (conventional commits; custom `enhance` group; `initial_tag = 0.1.0`). `CHANGELOG.md` — generated/maintained by the release workflow.
- `CLAUDE.md`, `.ai/` (logs), `.serena/` (config + memories), `.claude/`, `README.md`, `abmmeso/` (temporary reference, to be deleted), `venv/` (git-ignored, 3.11.15).

## Package layout (`src/mesoltm/`)
- `core/` — `base_link`, `link` (discrete LTM, verbatim), `connector_link`, `vehicle` (`Vehicle` carries `route`/`position`/`trajectory` **and `props`** — a free-form mutable metadata dict set at init and updatable any time; the core never touches it, downstream logic/plugins/`color_by` use it; must be JSON-serialisable to survive the history round-trip — plus `journeys`/`active` and `snapshot_journey()`/`reset_for_new_journey()` for multi-journey re-injection, see the multi-journey note below), `simulation` (`Simulation`: `run(progress=False)` = batch, `progress=True` shows a tqdm bar on stderr (off by default so batch runs stay byte-identical); also `start()`/`step()`/`current_step`/`total_steps` for step-driven control, and `inject(node_id, vehicle, at_time=None, check_reentry_node=True)` for dynamic demand (same vehicle re-injectable for further trips); **per-step history logging** via `record_history`/`history_path`/`history_classify` → `Simulation.history` (a `SimulationHistory`), `save_history(path)`); `core/nodes/` — `base_node` (routing seam), `one_to_one_node`, `diverge_node`, `merge_node`, `general_node_model`, `origin_node` (`add_trip` = injectable demand), `destination_node`.
- `network/` — `network` (builder: add_node/add_link/set_origin/set_destination/compile; auto O/D connectors; capacity-proportional priorities; `compile(injection_budget=N)` sizes connectors for dynamic injection (**default 100**; `NetworkState.inject` emits a `RuntimeWarning` if injections exceed the budget — the over-budget vehicle is held in its origin queue, never silently dropped); `compile(record_history=, history_path=, history_classify=)` enables the animation history log), `builders` (grid_network incl. partial, corridor_network, dict I/O), `state` (read-only NetworkState; also `inject(...)` splicing O/D connectors onto a real-link route; `waiting_vehicles(node)` = the live origin entry-queue vehicles, next to `entry_queue(node)` count; `remaining_real_route(vehicle, current_link=None)` = the real links of `vehicle.route` from the current link onward (reads the vehicle's own plan, never recomputes) — shared by `vehicles_in_network` and the recorder).
- `recording.py` (**matplotlib-free**, top-level so `core.simulation` can import it) — per-step animation history: `Frame`/`AgentSnapshot`/`WaitingSnapshot` dataclasses, `capture_frame(state, classify)`. **No route is ever computed at recording time**: each agent's remaining route is read verbatim from `vehicle.route` via `state.remaining_real_route` (the same helper the rerouting interface uses) and logged (`AgentSnapshot.route`/`WaitingSnapshot.route`); the next hop is read back from it. So the log always matches what actually ran, including exogenous mid-run reroutes. Pure node-by-node policy routing stores no plan on the vehicle, so its logged route is empty (honest — the recorder does not fabricate one). Snapshots also carry a copy of the vehicle's `props` metadata (`AgentSnapshot.props`/`WaitingSnapshot.props`), so a `color_by` callable or later analysis can use per-vehicle info; it round-trips through the JSON. In the JSON, each agent/waiting entry is a **keyed object** (field-name keys), not a positional array, so the log is self-describing (`AgentSnapshot(**obj)` rebuilds it); frame occupancy is under `link_occupancy`. `SimulationHistory` (geometry + frames; `save`/`load` JSON), `record_run(sim)` convenience. Snapshots store scalars/lists/dicts only. Exported from `mesoltm`.
- `routing/` — `policy` (RoutingPolicy protocol + StaticRoutePolicy default; `next_link(state: object)` to match the protocol), `shortest_path` (ShortestPathPolicy, networkx; `route(state, a, b)` = full-route planner; `refresh(state)` rebuilds the live graph once so a rerouting plugin can re-plan many vehicles in one step (build once, set `dynamic=False`, reuse); `cost` callback typed `Callable[[int, NetworkState], float]` for congestion-aware costs). `NetworkState` and `VehicleView` are exported from `mesoltm` for external typing.
- `plugins/` (renamed from `control/`) — `plugin`: the general per-step loop hook (run first each step), `Network.compile(..., plugins=[...])`. `Plugin` (base), `FunctionPlugin(fn)`, `ReroutingPlugin(reroute_fn)`. A plugin can change many aspects of the sim; rerouting needs no special hook because each `Vehicle` stores its own `route` and the network only propagates it, so rewriting `vehicle.route` reroutes that vehicle. `ReroutingPlugin` is the simple routing interface: gets `state.vehicles_in_network()` (`VehicleView`s), returns `{vehicle: real_route}` for only the ones to change (applied via `state.set_route`). No step-tracker plugin — the sim loop owns `state.step`.
- `demand/` — `demand.vehicles_from_demand_profile`.
- **Multiple journeys per vehicle (2026-07-10):** a `Vehicle` can complete several trips. Each finished trip is frozen at absorption (`Vehicle.snapshot_journey` in `DestinationNode.compute_flows`) into an immutable **journey record** stored on `vehicle.journeys` — the **single source of truth** for completed trips — and on the destination's `completed_journeys` (the *same* dict object). `Vehicle` also carries `active` (True while queued/en route, cleared at absorption; set in `OriginNode.add_trip`) and `reset_for_new_journey()` (clears live `trajectory`/`end`/`position`, not `route`). `Simulation.inject`/`NetworkState.inject` take `check_reentry_node=True` and re-inject the **same** vehicle for another trip, guarded by a mandatory still-active check (`RuntimeError`) and a default re-entry-node check (`ValueError` if it re-enters at a different **real** node than it last left — `NetworkState._journey_exit_node` ignores connector nodes). Caller sets `vehicle.route` to the new trip before re-injecting. All trip metrics read journey records, so static demand (one journey) and hand-injected/re-injected vehicles (many) share one accounting path. Demo: `examples/multi_trip_injection.py` (A→B→C, B both origin+dest).
- `metrics/` — `trips` (`collect_trips`, `trip_record`, `summarize_trips`, `write_trips_csv`): **per-journey** records keyed by `(vehicle_id, journey_index)`. `trip_record(journey, dt, …)` takes a **journey record** (not a live `Vehicle`); `collect_trips` aggregates each destination's `completed_journeys` (one row per completed journey); `write_trips_csv` has a `journey_index` column. Per-vehicle `route` driven plus three durations. **Connector time handling (2026-07-06):** each one-cell O/D connector has a free-flow crossing lag of exactly one step (T1=T2=1); that one step is an artifact and is **removed** from `travel_time` (so an empty/unrestricted connector adds nothing), but any time a vehicle spends on a connector **beyond** one step is a genuine supply-limited wait to enter/leave the network and is **kept**. `network_time` = sum of real (non-connector) trajectory segments only (**connector-free**); `travel_time` = `arrival − start − Σ(one free-flow step per connector crossed)`; `access_time` = `travel_time − network_time` (origin-queue wait + supply-limited connector wait). Invariant `travel_time == access_time + network_time` holds. Do **not** revert to `network_time = arrival − first_real_entry` (that wrongly counted the destination connector) nor to removing the whole connector crossing (that wrongly dropped supply-limited entry waits). Derived from `Vehicle.trajectory`, which the `Link` fills in `set_inflow`/`set_outflow` (behaviour-preserving; see the Deviations page §A6). The CLI path (`python -m mesoltm` / `mesoltm` console script) is covered by `tests/test_cli.py` and a `test.yml` CLI smoke step; the CLI shows the progress bar by default (`--no-progress` disables it).
- `io/` — `scenario` (JSON build/load/save); `cli.py` + `__main__.py` (`python -m mesoltm`).
- `visualizations/` — `_draw` (**shared** drawing primitives for the static map AND the video, so both stay in sync and code isn't duplicated: `PARALLEL_RAD`, node styling constants, `bezier_point` (matplotlib's exact `arc3` point), `fan_links(positions, endpoints) -> {lid: (p0,p1,rad)}` (lone link straight, shared pair fanned per-direction), `draw_arc`, `draw_nodes`; takes `ax`, no matplotlib import of its own). `plots`: cumulative curves; `plot_link_flow` (sums links into one cut curve) vs `plot_link_flows` (one labelled line per link); `plot_network` (colour links by flow/occupancy/density/capacity, optional `annotate_links` labels, colorbar; **uses the `_draw` helpers** so the map and the video render identical arcs/nodes — only link colour differs; robust to arbitrary node layouts and any number of links between a pair; labels sit on their arc via `bezier_point(...,0.5)`; arc apexes fed to `update_datalim` so bows aren't clipped; title padded above the plot); travel-time distribution; per-link travel-time bars; `plot_link_time_series` (one subplot per link: per-vehicle travel time vs entry time as a running average, to show congestion building up). `animation`: agent-movement video/frames from a `SimulationHistory` — `NetworkLayout` (`from_state`/`from_history`, spring-layout fallback; links fanned via the shared `_draw.fan_links` (same as `plot_network`), so the video and the static map draw identical arcs: a lone link straight, a shared pair on `arc3,rad=PARALLEL_RAD*(k+1)` per direction, opposing directions bowing to opposite sides, endpoints on the node centres. `_draw_backdrop` draws links/nodes via the shared `draw_arc`/`draw_nodes`; `_bezier_point` (= `_draw.bezier_point`) is matplotlib's **exact** `arc3` point, so agent slots lie on the very curve the link is drawn with (diagonal edges too); `min_link` for scale-aware sizing), `render_frame` (discrete FIFO queue slots; **sizes markers/nodes/labels to the network** so dense grids stay readable, with per-link **spacing-aware** marker shrink (`_fit_marker`) so queued agents never overlap; agents drawn as slightly smaller dots; `detail="auto"|"full"|"minimal"` + `show_agent_ids`/`show_next_link`/`show_node_labels` overrides — detail auto-drops when dense but is available on select; **`color_by`** = `"category"` (default) | `"next_link"` (read from the logged `AgentSnapshot.route`) | `None` (uniform, colouring off) | a **callable** `fn(snapshot) -> str` colouring by anything on the snapshot, esp. `snapshot.props`; agents **waiting** to enter (origin queue / source connector) are drawn as a **count badge on the node itself**, not as points, so they are never confused with travelling agents; Okabe-Ito palette; `highlight_links`), `save_frames` (per-step PNGs), `save_animation` (MP4 via ffmpeg / GIF via Pillow fallback; `fps` default 25; `subsample` = frames-per-step playback speed >1 slower / <1 faster; `frames_dir` also dumps PNGs), `animate_history`/`animate_from_history_file` (guides the user if the log is missing)/`animate_simulation`, `expand_frame_indices`, `resolve_palette`. matplotlib imported at module top; the package `__init__` does not import `visualizations`, so `import mesoltm` stays cheap (recording is separate + matplotlib-free).
- `tests/` — **inside the package**, relative imports: `test_regression` (exact match vs abmmeso golden), `test_core`, `test_network`, `test_routing`, `test_scenario`, `test_metrics`, `test_priorities`, `test_injection` (step/inject), `test_rerouting` (ReroutingPlugin), `test_recording` (history logging: remaining-route logged from `vehicle.route`, policy-routed vehicle logs no fabricated plan, log matches a mid-run reroute; matplotlib-free), `test_animation` (renderer + color_by category/next_link/None/callable, detail levels, opposing-link fan, `importorskip("matplotlib")`), `test_plots` (static `plot_network`: opposing links fanned onto distinct arcs, bowed arrows not clipped on a colinear layout, >2 parallel links on a non-aligned layout draw one distinct arc each; reads the `annotate` link arrows from `ax.texts[].arrow_patch`, `importorskip("matplotlib")`). `Vehicle.props` + props capture/round-trip covered in `test_core`/`test_recording`. `test_cli` covers the CLI entry point. `test_progress` covers `run(progress=True)` (tqdm bar on stderr) vs the silent default. `test_injection` also covers multi-journey re-injection (two journeys tracked + CSV `journey_index`, still-active `RuntimeError`, wrong-node `ValueError` + bypass). 79 tests total.

## Runtime flow
`Network.compile(...)` → `Simulation` of links + nodes → `.run()` → per-link cumulative
in/outflow arrays + arrived-trip records + per-vehicle trajectories (+ optional CSV via
CLI/scenario or `metrics.write_trips_csv`). Routing is per-vehicle via `vehicle.route`
or a `RoutingPolicy`; plugins run first each step.

Keep this current when files/modules are added, moved, or removed.
