# Demand

A helper for turning a time-varying demand rate profile into a list of `Vehicle`s
with staggered departure times (and optional route shares). See
[Building networks](../guide/building-networks.md) for how demand is attached to
an origin.

::: mesoltm.demand.demand.vehicles_from_demand_profile
